<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <!--
        System splash screen (Android 12+ / API 31+).
        Plain background matching the JS splash, with a transparent icon so no
        launcher icon flashes before our custom JS splash (SplashScreen.tsx)
        takes over. The native splash thus blends invisibly into the JS one.
    -->
    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_transparent</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

</resources>
